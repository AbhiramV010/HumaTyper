cmake_minimum_required(VERSION 3.16)
project(HumaTyperInstaller CXX)

if (NOT WIN32)
    message(FATAL_ERROR "This installer targets Windows only.")
endif()

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

add_executable(HumaTyperInstaller main.cpp)
target_compile_definitions(HumaTyperInstaller PRIVATE UNICODE _UNICODE)

target_link_libraries(HumaTyperInstaller PRIVATE
    shell32
    ole32
    advapi32
    uuid
    user32
)

if (MINGW)
    target_link_options(HumaTyperInstaller PRIVATE -municode)
endif()
